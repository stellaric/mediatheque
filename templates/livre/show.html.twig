{% extends 'base.html.twig' %}


{% block body %}
{% include "admin/header.html.twig" %}
   <main class="main_livre">

   <div class="card white rounded-2 shadow-1 mx-auto" style="max-width: 400px;">
  <div class="card-image">
    <img src="{{ livre.imgLivre }}" class="responsive-media" alt="{{ livre.titre }}" style="height:300px;">
  </div>
          <div class="card-content">
           <span class="h3">{{ livre.titre }}</span>
             <div class="font-s3 text-black text-light-2">By {{ livre.auteur }}</div>
            <div class="mt-3"> <p>{{ livre.description }}</p>
            <p>{{ livre.dateParution ? livre.dateParution|date('Y-m-d') : '' }}</p>
            <p>{{ livre.genre }}</p>
          </div>
  </div>
  <div class="card-footer d-flex mt-1 ">
    <button data-waves="" class="btn mole light-1  text-center text-white rounded-1">  <a href="{{ path('app_livre_edit', {'id': livre.id}) }}">edit</a></button>
  {{ include('livre/_delete_form.html.twig') }}

 </div>
</div>
   <div class="card white rounded-2 shadow-1 mx-auto text-center" style="max-width: 400px;">
 <a href="{{ path('app_livre_index') }}">back to list</a>

  </div>

   </main>
     {% include "accueil/footer.html.twig" %}
{% endblock %}
